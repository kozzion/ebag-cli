# ebag-cli

A Python command-line tool. (Purpose TBD.)

## Status

Early scaffolding — implementation TBD.

## Quick start

```bash
# Clone
git clone https://github.com/kozzion/ebag-cli.git
cd ebag-cli

# (Recommended) create a virtualenv
python -m venv .venv
# On Windows:
.venv\Scripts\activate
# On macOS / Linux:
source .venv/bin/activate

# Install dependencies (once added)
pip install -r requirements.txt
```

## Usage

```bash
# Example (not yet implemented)
ebag <args>
```

## Development

```bash
# Run tests
pytest

# Lint / format (suggested)
ruff check .
ruff format .
```

## License

TBD.
